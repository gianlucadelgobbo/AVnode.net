extends ../layout
block teaser
  if data.teaserImageFormats.large
    .d-flex.justify-content-center.align-items-start
      img.img-fluid(
        src=data.teaserImageFormats.large
        alt=data.stagename
      )

block title
  .performer_header_wrapper
    .container
      .row
        .col-lg-6
          h1.main_title=data.stagename          
          ul.performer_address.list-inline
            each address in data.addresses
              li.list-inline-item=address.locality + ", " + address.country
          div.performer_stats_wrapper
            ul.performer_stats_list.performer_stats_list_1.list-inline
              if data.stats.events
                li.list-inline-item!=__('Events')+" (<span>"+data.stats.events+"</span>)"
              if data.stats.performances
                li.list-inline-item!=__('Performances')+" (<span>"+data.stats.performances+"</span>)"  
              if data.stats.crews
                li.list-inline-item!=__('Crews')+" (<span>"+data.stats.crews+"</span>)"    
            ul.performer_stats_list.performer_stats_list_2.list-inline
              if data.stats.footage
                li.list-inline-item!=__('Footage')+" (<span>"+data.stats.footage+"</span>)"
              if data.stats.playlists
                li.list-inline-item!=__('Playlists')+" (<span>"+data.stats.playlists+"</span>)"  
              if data.stats.tvshows
                li.list-inline-item!=__('Tvshows')+" (<span>"+data.stats.tvshows+"</span>)"    
              li.list-inline-item!=__('Gallery')+" (<span>"+data.stats.galleries+"</span>)"    
              
        
        
        .col-lg-6
          .row
            .col-sm-6            
              img.rounded-circle.img-fluid.miss(
                src=data.imageFormats.medium
                alt=data.stagename
              )
            .col-sm-6
              img.img-fluid(src="/images/logo_avnode_svg.svg")

  .container
    .row
      .col-sm-12
        .performer_map_wrapper
          h2="auto centred Google map: a marker for each performer's address"
    
  if data.abouts
    .abouts_wrapper
      .container
        .row
          .col-sm-12
              .about_wrapper
                !=data.about        
              .more_button
                span.color1.lnr.lnr-plus-circle
    .abouts_divider




block content
  .row.performer_events_wrapper
    .col-lg-12
      h2.main_title.indented_title.color1!=__('Events')+" <small>(<span>"+data.stats.events+"</span>)</small>"
      .row
        each item in data.events
          .col-sm-4
            .event_item_wrapper
              img.img-fluid(
                src=item.imageFormats.medium
                alt=item.title
              )
              .event_item_info_wrapper
                h4.event_item_title=item.title
                ul.event_item_date_and_locality.list-inline
                  li.list-inline-item.event_item_date=item.boxDate
                  li.list-inline-item.event_item_locality=item.boxVenue
  br
  br
  .row
    .col-lg-12
      h3=__('Crews')
      .card-deck
        each item in data.crews
          .card
            if item.imageFormats
              img.card-img-top.img-fluid(
                src=item.imageFormats.medium
                alt=item.stagename
              )
            .card-block
              h4.event_item_title=item.stagename
            .card-block.pt-0
              a.btn.btn-secondary(href=item.publicUrl)=__('More')
  br
  br
  .row
    .col-lg-12
      h3=__('Performances')
      .card-deck
        each item in data.performances
          .card
            if item.imageFormats
              img.card-img-top.img-fluid(
                src=item.imageFormats.medium
                alt=item.title
              )
            .card-block
              h4.event_item_title=item.title
            .card-block.pt-0
              a.btn.btn-secondary(href=item.publicUrl)=__('More')
  br
  br
  .row
    .col-lg-12
      h3=__('TV Shows')
      .card-deck
        each item in data.tvshows
          .card
            if item.imageFormats
              img.card-img-top.img-fluid(
                src=item.imageFormats.large
                alt=item.title
              )
            .card-block
              h4.event_item_title=item.title
            .card-block.pt-0
              a.btn.btn-secondary(href=item.publicUrl)=__('More')
  br
  br
  .row
    .col-lg-12
      h3=__('Footage')
      .card-deck
        each item in data.footage
          .card
            if item.imageFormats
              img.card-img-top.img-fluid(
                src=item.imageFormats.large
                alt=item.title
              )
            .card-block
              h4.event_item_title=item.title
            .card-block.pt-0
              a.btn.btn-secondary(href=item.publicUrl)=__('More')
  br
  br
  .row
    .col-lg-12
      h3=__('Playlists')
      .card-deck
        each item in data.playlists
          .card
            if item.imageFormats
              img.card-img-top.img-fluid(
                src=item.imageFormats.large
                alt=item.title
              )
            .card-block
              h4.event_item_title=item.title
              h6.card-subtitle.text-muted=item.subtitle
            .card-block.pt-0
              a.btn.btn-secondary(href=item.publicUrl)=__('More')
  br
  br
  .row
    .col-lg-12
      h3=__('Galleries')
      .card-deck
        each item in data.galleries
          .card
            if item.imageFormats
              img.card-img-top.img-fluid(
                src=item.imageFormats.medium
                alt=item.title
              )
            .card-block
              h4.event_item_title=item.title
            .card-block.pt-0
              a.btn.btn-secondary(href=item.publicUrl)=__('More')
