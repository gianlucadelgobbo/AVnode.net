extends ../layout
block topmenu
  include topmenu
block content
  b!="Your organizations"
  if data.length
    ol
      each item, index in data
        li
          a(href="/admindev/partners/"+item._id+"/")!=item.stagename
