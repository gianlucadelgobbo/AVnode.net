if messages.errors
  .container
    .alert.alert-danger
      for error in messages.errors
        div=error.msg
if messages.info
  .container
    .alert.alert-info
      for info in messages.info
        div=info.msg
if messages.success
  .container
    .alert.alert-success
      for success in messages.success
        div=success.msg
