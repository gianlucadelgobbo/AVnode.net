.video_item_wrapper
  if item.imageFormats
    a(href="/videos/"+item.slug, title=item.title)
      img.card-img-top.img-fluid(
        src=item.imageFormats.large
        alt=item.title
      )
  .video_item_info_wrapper
    a(href="/videos/"+item.slug, title=item.title)
      h4.video_item_title=item.title
    if item.categories
      ul.list-inline.category_video_item
        each category in item.categories
          li.list-inline-item=category.name
    if item.users
      .users_video_item_wrapper
        each user in item.users
          div
            img(src=user.imageFormats.small, alt=user.stagename)
          div
            p.users_video_item_name=user.stagename
            p.users_video_item_views=item.stats.visits+" "+__("views")
        .users_video_item_likes
          span.color2.lnr.lnr-heart
          br
          span=item.stats.likes+" "+__("likes")