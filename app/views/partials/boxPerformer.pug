.performer_item_wrapper
  if item.imageFormats
    a(href="/"+item.slug, stagename=item.stagename, title=item.stagename)
      img.card-img-top.img-fluid(
        src=item.imageFormats.large
        alt=item.stagename
      )
  .performer_item_info_wrapper
    a(href="/"+item.slug, stagename=item.stagename, title=item.stagename)
      h4.performer_item_title=item.stagename
    ul.list-inline.performer_infos_list
      //if item.stats.performances      
          li.list-inline-item!="<span>"+item.stats.performances+"</span> "+global.__('Performances')
        if item.stats.events      
          li.list-inline-item!="<span>"+item.stats.events+"</span> "+global.__('Events')
        if item.stats.news      
          li.list-inline-item!="<span>"+item.stats.news+"</span> "+global.__('News')
      if item.stats["av-performance"]      
        li.list-inline-item!="<span>"+item.stats["av-performance"]+"</span> "+global.__('AV Performances')
      if item.stats["mapping"]      
        li.list-inline-item!="<span>"+item.stats["mapping"]+"</span> "+global.__('Mappings')
      if item.stats["vj-set"]      
        li.list-inline-item!="<span>"+item.stats["vj-set"]+"</span> "+global.__('VJ Sets')
      if item.stats["video-installation"]      
        li.list-inline-item!="<span>"+item.stats["video-installation"]+"</span> "+global.__('AV Installations')
      if item.stats["workshop"]      
        li.list-inline-item!="<span>"+item.stats["workshop"]+"</span> "+global.__('Workshops')
      if item.stats["lecture"]      
        li.list-inline-item!="<span>"+item.stats["lecture"]+"</span> "+global.__('Lectures')
      if item.stats["project-showcase"]      
        li.list-inline-item!="<span>"+item.stats["project-showcase"]+"</span> "+global.__('Project showcases')
      if item.stats["lights-installation"]      
        li.list-inline-item!="<span>"+item.stats["lights-installation"]+"</span> "+global.__('Lights installations')
      if item.stats["dj-set"]      
        li.list-inline-item!="<span>"+item.stats["dj-set"]+"</span> "+global.__('DJ Sets')
      if item.stats.videos      
        li.list-inline-item!="<span>"+item.stats.videos+"</span> "+global.__('Videos')
      if item.stats.galleries      
        li.list-inline-item!="<span>"+item.stats.galleries+"</span> "+global.__('Galleries')
      if item.stats.footage      
        li.list-inline-item!="<span>"+item.stats.footage+"</span> "+global.__('Footage')
      if item.stats.playlists      
        li.list-inline-item!="<span>"+item.stats.playlists+"</span> "+global.__('Playlists')
