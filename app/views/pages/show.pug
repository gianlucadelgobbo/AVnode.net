extends ../layout

block content
  .container
    .row
      .col-lg-8
        !=data.post_content
      .col-lg-4
        if data.featured && data.featured.thumbnail
          .mb-3
            img.card-img-top.img-fluid(
              src=data.featured.thumbnail
              alt=data.post_name
            )