import { h } from 'preact';
import { connect } from 'preact-redux';
import { injectIntl, FormattedMessage } from 'preact-intl';

const EventImagesContainer = injectIntl(() => {
  return (
    <div>
        <label>
        EventImagesContainer
        </label>
    </div>
  );
});

export default EventImagesContainer;