import React from "react";
import { FormattedMessage } from "react-intl";

export const FORM_NAME = "EVENTS.PUBLIC";
export const SECTION = "EVENT";
export const EVENT_NAME = (
  <FormattedMessage id="event.public.data" defaultMessage="EVENT PUBLIC DATA" />
);
export const SHOW = <FormattedMessage id="show" defaultMessage="SHOW" />;
