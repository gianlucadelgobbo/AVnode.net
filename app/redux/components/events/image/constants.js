import React from 'react';
import {FormattedMessage} from 'react-intl';

export const FORM_NAME = "EVENTS.IMAGE";
export const EVENT_NAME =   <FormattedMessage
                                id="event.image"
                                defaultMessage="EVENT IMAGE"
                            />
export const SHOW = <FormattedMessage
                            id="show"
                            defaultMessage="SHOW"
                            />
