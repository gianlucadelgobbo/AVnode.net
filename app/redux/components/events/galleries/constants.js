import React from 'react';
import {FormattedMessage} from 'react-intl';

export const FORM_NAME = "EVENTS.GALLERIES";
export const EVENT_NAME =   <FormattedMessage
                                id="event.galleries"
                                defaultMessage="EVENT GALLERIES"
                            />
export const SHOW = <FormattedMessage
                            id="show"
                            defaultMessage="SHOW"
                            />
